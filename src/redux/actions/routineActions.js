import database from '@react-native-firebase/database';
import {ADD_ROUTINE_SUCCESS, ADD_ROUTINE_FAILED} from '../constants';

export const addRoutine = (routine, uid) => {
  console.log(routine, 'add routine action');
  return dispatch => {
    try {
    } catch (error) {}
  };
};
